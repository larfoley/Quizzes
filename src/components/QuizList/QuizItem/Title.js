import styled from 'styled-components'

const Title = styled.h2`
  margin: 0;
  font-size: 1.4em;
  ${'' /* overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap; */}
`

export default Title
